# Local development: the Express API running on your machine.
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Production: your deployed API, with the /api suffix.
# Baked into the bundle at BUILD time, so changing it needs a redeploy.
# NEXT_PUBLIC_API_URL=https://excel-connect-hub-api.onrender.com/api
